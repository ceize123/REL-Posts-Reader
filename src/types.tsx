export interface IParams {
	id: number;
}

export interface Posts {
	userId: number;
	id: number;
	title: string;
	body: string;
}

export interface Post {
	postId: number;
	id: number;
	name: string;
	email: string;
	body: string;
}